# CPU-only torch. Installed first, from the CPU wheel index, so that
# sentence-transformers does not drag in the ~2.5GB CUDA build on a machine
# that has no GPU.
--index-url https://download.pytorch.org/whl/cpu
torch>=2.6.0
